/**
 * Define la estructura principal de un User dentro del dominio,
 * sin incluir detalles técnicos (por ejemplo, llamadas HTTP o configuración de Angular).
 */
export interface User {
    id: number;
    username: string;
    password: string;
  }
  